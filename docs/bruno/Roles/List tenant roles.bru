meta {
  name: List tenant roles
  type: http
  seq: 1
}

get {
  url: {{BASE_URL}}/admin/role/{{tenant}}
  body: none
  auth: inherit
}

params:path {
  tenant: acme-corp
}

params:query {
  ~p: 1
  ~i: 10
  ~f: 
  ~aresource: 
  ~adescription: 
  ~exclude_global: false
}

docs {
  List roles defined in a specific tenant.
  
  Supports pagination, filtering by resource, and description search.
}
